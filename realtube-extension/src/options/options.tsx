// RealTube Options Page
// This will be expanded in Step 38

import React from "react";
import { createRoot } from "react-dom/client";

function Options() {
  return <div>RealTube Options</div>;
}

const root = createRoot(document.getElementById("root")!);
root.render(<Options />);
