# RealTube Environment Variables
# Copy this file to .env and fill in real values:
#   cp .env.example .env

# PostgreSQL identity (password is managed via Docker secrets)
POSTGRES_USER=realtube
POSTGRES_DB=realtube

# The database password lives in secrets/postgres_password (file-based Docker secret).
# Create it with:  echo 'your-password' > secrets/postgres_password
#
# For local dev without Docker secrets, you can set DATABASE_URL directly:
# DATABASE_URL=postgres://realtube:password@localhost:5432/realtube

# Redis password (leave empty for no auth in dev; S3 will add requirepass)
# REDIS_PASSWORD=
